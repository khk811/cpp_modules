#ifndef WHATEVER_HPP
#define WHATEVER_HPP

// header 파일 내 템플릿 정의를 이렇게 하는지 모르겠음
// 뭔가 다른 방법이 있을거 같은데 이게 맞나 싶음.햣

template<typename T>
void	swap(T& a, T& b);

template<typename T>
T&		min(T& a, T& b);

template<typename T>
T&		max(T& a, T& b);

#endif

